package com.example.salus.ui.theme

import androidx.compose.ui.graphics.Color

// === Cores Base ===
val FundoClaro = Color(0xFFE9E9E9)
val FundoEscuro = Color(0xFF555C6B) // Modo Escuro
val Branco = Color(0xFFFFFFFF)
val Preto = Color(0xFF000000)
val CinzaIcones = Color(0xFF757575) // Ícones inativos da BottomBar

// === Cores de Texto ===
val TextoPrimarioClaro = Color(0xFF1C1B1F) // Texto principal sobre fundo claro (calculado, pode ajustar)
val TextoSecundarioClaro = Color(0xFF79747E) // Texto secundário sobre fundo claro (calculado, pode ajustar)
val TextoPrimarioEscuro = Color(0xFFFFFFFF) // Texto principal sobre fundo escuro
// val TextoSecundarioEscuro = Color(0xFFCAC4D0) // Exemplo, podemos ajustar

// === Cores Específicas de Elementos (Login/Auth) ===
val AuthIconeCampo = Color(0xFF4F92EB) // Ícones e texto placeholder nos campos
val AuthBotaoTextoSecundario = Color(0xFF2D3D60) // Texto "Esqueceu Senha", Botão Login

// === Gradientes (Cores Início e Fim) ===
// Azul
val AzulGradienteInicio = Color(0xFF4C629B)
val AzulGradienteFim = Color(0xFF7089C4)
// Vermelho (Alerta Botão/UI)
val VermelhoGradienteInicio = Color(0xFFFF2D36)
val VermelhoGradienteFim = Color(0xFFF48B1E)

// === Cores Específicas de Elementos (App Principal) ===
val CardBranco = Color(0xFFFFFFFF) // Botões remédios/maps/chatbot, cards em geral
val CalendarioFundo = Color(0xFFF7F4F1)
val BotaoAdicionar = Color(0xFF84C155) // Botão Add Remédio/Consulta, Botão Entendido Selecionado
val GraficoDiaSelecionado = Color(0xFFFB975B)
val GraficoDiaNaoSelecionado = Color(0xFF01698C)

// === Cores da Tela de Alerta ===
val AlertaFundoVermelho = Color(0xFFFF0000)
val AlertaIconeAmarelo = Color(0xFFFFBE00)
val AlertaInfoFundo = Color(0xFFE9E9E9) // Quadrado de informação
val AlertaInfoTextoIcone = Color(0xFF384C78) // Escritas/icone/bpm no quadrado
val AlertaBotaoEntendidoFundo = Color(0xFFE9E9E9)
val AlertaBotaoEntendidoTexto = Color(0xFF384C78)